-- ============================================================
--  SERAPHICA CLIENT v15.2 - NO LOADING
--  WELCOME + FULL MENU
-- ============================================================

print("[SeraphiCA] Script started")

-- [[ ИМПОРТ СЕРВИСОВ ]]
local Players = game:GetService("Players")
local UserInputService = game:GetService("UserInputService")
local TweenService = game:GetService("TweenService")
local Workspace = game:GetService("Workspace")
local ReplicatedStorage = game:GetService("ReplicatedStorage")
local RunService = game:GetService("RunService")
local Lighting = game:GetService("Lighting")

print("[SeraphiCA] Services loaded")

-- [[ ОЖИДАНИЕ ЗАГРУЗКИ ]]
if not game:IsLoaded() then
    print("[SeraphiCA] Waiting for game load...")
    game.Loaded:Wait()
end
print("[SeraphiCA] Game loaded")

-- [[ ПОЛУЧЕНИЕ ИГРОКА ]]
local LocalPlayer = Players.LocalPlayer
if not LocalPlayer then
    print("[SeraphiCA] Waiting for LocalPlayer...")
    Players.PlayerAdded:Wait()
    LocalPlayer = Players.LocalPlayer
end
print("[SeraphiCA] LocalPlayer: " .. LocalPlayer.Name)

local Character = LocalPlayer.Character
if not Character then
    print("[SeraphiCA] Waiting for Character...")
    Character = LocalPlayer.CharacterAdded:Wait()
end
local Humanoid = Character:WaitForChild("Humanoid")
local PlayerGui = LocalPlayer:WaitForChild("PlayerGui")

print("[SeraphiCA] Character ready")

-- [[ ОЧИСТКА СТАРЫХ GUI ]]
for _, gui in ipairs(PlayerGui:GetChildren()) do
    if gui.Name:match("SeraphiCA") then
        pcall(function() gui:Destroy() end)
        print("[SeraphiCA] Removed old GUI: " .. gui.Name)
    end
end

-- [[ СОЗДАНИЕ SCREEN GUI ]]
local ScreenGui = Instance.new("ScreenGui")
ScreenGui.Name = "SeraphiCA_GUI"
ScreenGui.ResetOnSpawn = false
ScreenGui.ZIndexBehavior = Enum.ZIndexBehavior.Sibling
ScreenGui.Parent = PlayerGui
print("[SeraphiCA] ScreenGui created")

-- ============================================================
--  ЦВЕТА
-- ============================================================
local C = {
    Primary = Color3.fromRGB(130, 200, 255),
    Secondary = Color3.fromRGB(210, 140, 255),
    Success = Color3.fromRGB(100, 255, 140),
    Error = Color3.fromRGB(255, 90, 90),
    Warning = Color3.fromRGB(255, 205, 60),
    Dark = Color3.fromRGB(8, 8, 28),
    Light = Color3.fromRGB(230, 240, 255),
    Gold = Color3.fromRGB(255, 215, 0),
    Purple = Color3.fromRGB(190, 130, 255),
    Cyan = Color3.fromRGB(0, 255, 240),
    White = Color3.fromRGB(255, 255, 255),
    Gray = Color3.fromRGB(160, 165, 175),
    Orange = Color3.fromRGB(255, 165, 0),
    Darker = Color3.fromRGB(5, 5, 20)
}

-- ============================================================
--  ШИФРОВАНИЕ
-- ============================================================
local function EncryptKey()
    local key = "081324"
    local encoded = ""
    for i = 1, #key do
        encoded = encoded .. string.char(string.byte(key, i) + 7)
    end
    return encoded
end

local function DecryptKey(encrypted)
    local decoded = ""
    for i = 1, #encrypted do
        decoded = decoded .. string.char(string.byte(encrypted, i) - 7)
    end
    return decoded
end

local ENCRYPTED_KEY = EncryptKey()
local ATTEMPTS = 0
local MAX_ATTEMPTS = 5
local SECRET_UNLOCKED = false
local LAST_TAB = 1
local secretPopup = nil

-- ============================================================
--  ФУНКЦИИ
-- ============================================================
local function Clamp(val, min, max)
    return math.max(min, math.min(max, val))
end

local function GetKyivTime()
    local now = os.time()
    local date = os.date("*t", now)
    local year = date.year
    local marchLS = 31 - (os.date("*t", os.time{year=year, month=3, day=31}).wday % 7)
    local octLS = 31 - (os.date("*t", os.time{year=year, month=10, day=31}).wday % 7)
    local dstStart = os.time{year=year, month=3, day=marchLS, hour=3}
    local dstEnd = os.time{year=year, month=10, day=octLS, hour=4}
    local isDST = (now >= dstStart and now < dstEnd)
    local offset = isDST and 3 or 2
    return os.date("!%H:%M:%S", now + offset * 3600)
end

local function GetAUTItems()
    return {
        "Sword", "Shield", "Potion", "Cursed Orb", "Mythical Gem",
        "Legendary Blade", "Dark Essence", "Light Fragment", "Void Stone",
        "Dragon Scale", "Phoenix Feather", "Shadow Cloak", "Crystal Heart",
        "Demon Horn", "Angel Wing", "Chaos Crystal", "Starlight Shard",
        "Abyssal Pearl", "Eldritch Ring", "Frozen Tear"
    }
end

local function TeleportToDesert()
    if Character and Character.PrimaryPart then
        Character.PrimaryPart.CFrame = CFrame.new(Vector3.new(0, 10, 0))
        print("[SeraphiCA] Teleported to desert")
    end
end

-- ============================================================
--  WELCOME ЭКРАН
-- ============================================================
local WelcomeFrame = Instance.new("Frame")
WelcomeFrame.Name = "SeraphiCA_Welcome"
WelcomeFrame.Size = UDim2.new(1, 0, 1, 0)
WelcomeFrame.BackgroundColor3 = Color3.fromRGB(5, 5, 20)
WelcomeFrame.BackgroundTransparency = 0.98
WelcomeFrame.BorderSizePixel = 0
WelcomeFrame.Visible = true
WelcomeFrame.ZIndex = 60
WelcomeFrame.Parent = ScreenGui

local wGrad = Instance.new("UIGradient")
wGrad.Color = ColorSequence.new({
    ColorSequenceKeypoint.new(0, Color3.fromRGB(10, 5, 30)),
    ColorSequenceKeypoint.new(0.5, Color3.fromRGB(30, 15, 60)),
    ColorSequenceKeypoint.new(1, Color3.fromRGB(10, 5, 30))
})
wGrad.Rotation = 45
wGrad.Parent = WelcomeFrame

-- Звезды
for i = 1, 30 do
    local star = Instance.new("Frame")
    star.Size = UDim2.new(0, math.random(2, 4), 0, math.random(2, 4))
    star.Position = UDim2.new(0, math.random(0, 1920), 0, math.random(0, 1080))
    star.BackgroundColor3 = C.White
    star.BackgroundTransparency = 0.4 + math.random() * 0.5
    star.BorderSizePixel = 0
    star.Parent = WelcomeFrame
    local corner = Instance.new("UICorner", star)
    corner.CornerRadius = UDim.new(0, 5)
end

-- Луна
local wMoon = Instance.new("ImageLabel")
wMoon.Size = UDim2.new(0, 120, 0, 120)
wMoon.Position = UDim2.new(0.5, -60, 0.2, -60)
wMoon.BackgroundTransparency = 1
wMoon.Image = "rbxassetid://1313579923"
wMoon.ImageColor3 = Color3.fromRGB(200, 220, 255)
wMoon.ImageTransparency = 0.3
wMoon.Parent = WelcomeFrame

spawn(function()
    while wMoon.Parent and WelcomeFrame.Visible do
        wMoon.Rotation = wMoon.Rotation + 0.3
        wait(0.05)
    end
end)

-- Кнопка закрытия Welcome
local wCloseBtn = Instance.new("TextButton")
wCloseBtn.Size = UDim2.new(0, 40, 0, 40)
wCloseBtn.Position = UDim2.new(1, -55, 0, 15)
wCloseBtn.BackgroundColor3 = C.Error
wCloseBtn.BackgroundTransparency = 0.1
wCloseBtn.BorderSizePixel = 0
wCloseBtn.Text = "✕"
wCloseBtn.TextColor3 = C.White
wCloseBtn.TextSize = 20
wCloseBtn.Font = Enum.Font.GothamBold
wCloseBtn.ZIndex = 70
wCloseBtn.Parent = WelcomeFrame
local wcCorner = Instance.new("UICorner", wCloseBtn)
wcCorner.CornerRadius = UDim.new(0, 20)
wCloseBtn.MouseButton1Click:Connect(function()
    WelcomeFrame.Visible = false
end)

-- WELCOME текст
local wText = Instance.new("TextLabel")
wText.Size = UDim2.new(1, 0, 0, 70)
wText.Position = UDim2.new(0, 0, 0.3, 0)
wText.BackgroundTransparency = 1
wText.Text = "WELCOME"
wText.TextColor3 = Color3.fromRGB(200, 230, 255)
wText.TextSize = 48
wText.Font = Enum.Font.GothamBold
wText.TextXAlignment = Enum.TextXAlignment.Center
wText.TextYAlignment = Enum.TextYAlignment.Center
wText.TextStrokeTransparency = 0
wText.TextStrokeColor3 = Color3.fromRGB(80, 150, 255)
wText.Parent = WelcomeFrame

-- Имя игрока
local wName = Instance.new("TextLabel")
wName.Size = UDim2.new(1, 0, 0, 50)
wName.Position = UDim2.new(0, 0, 0.43, 0)
wName.BackgroundTransparency = 1
wName.Text = LocalPlayer.Name
wName.TextColor3 = Color3.fromRGB(100, 220, 255)
wName.TextSize = 34
wName.Font = Enum.Font.GothamSemibold
wName.TextXAlignment = Enum.TextXAlignment.Center
wName.TextYAlignment = Enum.TextYAlignment.Center
wName.TextStrokeTransparency = 0.3
wName.TextStrokeColor3 = Color3.fromRGB(0, 100, 255)
wName.Parent = WelcomeFrame

-- Admin
local wAdmin = Instance.new("TextLabel")
wAdmin.Size = UDim2.new(1, 0, 0, 35)
wAdmin.Position = UDim2.new(0, 0, 0.53, 0)
wAdmin.BackgroundTransparency = 1
wAdmin.Text = "Admin Telegram: @Mpmtww"
wAdmin.TextColor3 = Color3.fromRGB(200, 180, 255)
wAdmin.TextSize = 18
wAdmin.Font = Enum.Font.Gotham
wAdmin.TextXAlignment = Enum.TextXAlignment.Center
wAdmin.TextYAlignment = Enum.TextYAlignment.Center
wAdmin.TextStrokeTransparency = 0.5
wAdmin.TextStrokeColor3 = Color3.fromRGB(150, 100, 255)
wAdmin.Parent = WelcomeFrame

-- Подпись
local wSub = Instance.new("TextLabel")
wSub.Size = UDim2.new(1, 0, 0, 30)
wSub.Position = UDim2.new(0, 0, 0.6, 0)
wSub.BackgroundTransparency = 1
wSub.Text = "SeraphiCA System Activated"
wSub.TextColor3 = Color3.fromRGB(150, 180, 220)
wSub.TextSize = 16
wSub.Font = Enum.Font.Gotham
wSub.TextXAlignment = Enum.TextXAlignment.Center
wSub.TextYAlignment = Enum.TextYAlignment.Center
wSub.TextStrokeTransparency = 0.7
wSub.Parent = WelcomeFrame

-- DEVELOPERS
local devFrame = Instance.new("Frame")
devFrame.Size = UDim2.new(0, 300, 0, 80)
devFrame.Position = UDim2.new(0.5, -150, 0.72, 0)
devFrame.BackgroundColor3 = Color3.fromRGB(20, 10, 50)
devFrame.BackgroundTransparency = 0.2
devFrame.BorderSizePixel = 1
devFrame.BorderColor3 = C.Purple
devFrame.Parent = WelcomeFrame
local dCorner = Instance.new("UICorner", devFrame)
dCorner.CornerRadius = UDim.new(0, 12)

local devTitle = Instance.new("TextLabel")
devTitle.Size = UDim2.new(1, 0, 0, 30)
devTitle.Position = UDim2.new(0, 0, 0, 0)
devTitle.BackgroundTransparency = 1
devTitle.Text = "РАЗРАБОТЧИКИ"
devTitle.TextColor3 = C.Gold
devTitle.TextSize = 16
devTitle.Font = Enum.Font.GothamBold
devTitle.TextXAlignment = Enum.TextXAlignment.Center
devTitle.TextYAlignment = Enum.TextYAlignment.Center
devTitle.Parent = devFrame

local devNames = Instance.new("TextLabel")
devNames.Size = UDim2.new(1, 0, 0, 40)
devNames.Position = UDim2.new(0, 0, 0.3, 0)
devNames.BackgroundTransparency = 1
devNames.Text = "seraphim × NotAbob"
devNames.TextColor3 = Color3.fromRGB(200, 180, 255)
devNames.TextSize = 18
devNames.Font = Enum.Font.GothamSemibold
devNames.TextXAlignment = Enum.TextXAlignment.Center
devNames.TextYAlignment = Enum.TextYAlignment.Center
devNames.Parent = devFrame

local devSub = Instance.new("TextLabel")
devSub.Size = UDim2.new(1, 0, 0, 20)
devSub.Position = UDim2.new(0, 0, 0.65, 0)
devSub.BackgroundTransparency = 1
devSub.Text = "жду идеального клиента"
devSub.TextColor3 = C.Gray
devSub.TextSize = 12
devSub.Font = Enum.Font.GothamItalic
devSub.TextXAlignment = Enum.TextXAlignment.Center
devSub.TextYAlignment = Enum.TextYAlignment.Center
devSub.Parent = devFrame

-- ============================================================
--  ВОДЯНОЙ ЗНАК
-- ============================================================
local Watermark = Instance.new("TextButton")
Watermark.Name = "SeraphiCA_Watermark"
Watermark.Size = UDim2.new(0, 140, 0, 32)
Watermark.Position = UDim2.new(0, 10, 0, 10)
Watermark.BackgroundColor3 = Color3.fromRGB(10, 10, 40)
Watermark.BackgroundTransparency = 0.15
Watermark.BorderSizePixel = 1
Watermark.BorderColor3 = C.Primary
Watermark.Text = "✦ SERAPHICA"
Watermark.TextColor3 = C.Primary
Watermark.TextSize = 13
Watermark.Font = Enum.Font.GothamBold
Watermark.ZIndex = 200
Watermark.Visible = true
Watermark.Parent = ScreenGui
local wmCorner = Instance.new("UICorner", Watermark)
wmCorner.CornerRadius = UDim.new(0, 8)

-- ============================================================
--  ОСНОВНОЕ МЕНЮ
-- ============================================================
local menuOpen = false
local menuFrame = nil

local function CreateMenu()
    print("[SeraphiCA] Creating menu...")
    
    local menuW = 620
    local menuH = 420
    local currentTab = 1
    local tabButtons = {}
    local tabContents = {}
    
    menuFrame = Instance.new("Frame")
    menuFrame.Name = "SeraphiCA_Menu"
    menuFrame.Size = UDim2.new(0, menuW, 0, menuH)
    menuFrame.Position = UDim2.new(0.5, -menuW/2, 0.5, -menuH/2)
    menuFrame.BackgroundColor3 = Color3.fromRGB(8, 8, 30)
    menuFrame.BackgroundTransparency = 0.05
    menuFrame.BorderSizePixel = 1
    menuFrame.BorderColor3 = C.Primary
    menuFrame.Visible = false
    menuFrame.ClipsDescendants = true
    menuFrame.ZIndex = 150
    menuFrame.Parent = ScreenGui
    
    local mainCorner = Instance.new("UICorner", menuFrame)
    mainCorner.CornerRadius = UDim.new(0, 16)
    
    -- Заголовок
    local titleBar = Instance.new("Frame")
    titleBar.Size = UDim2.new(1, 0, 0, 40)
    titleBar.BackgroundColor3 = Color3.fromRGB(15, 15, 45)
    titleBar.BackgroundTransparency = 0.2
    titleBar.BorderSizePixel = 0
    titleBar.Parent = menuFrame
    local tCorner = Instance.new("UICorner", titleBar)
    tCorner.CornerRadius = UDim.new(0, 16)
    
    local titleText = Instance.new("TextLabel")
    titleText.Size = UDim2.new(1, -60, 1, 0)
    titleText.Position = UDim2.new(0, 30, 0, 0)
    titleText.BackgroundTransparency = 1
    titleText.Text = "✦ SERAPHICA CLIENT ✦"
    titleText.TextColor3 = C.Light
    titleText.TextSize = 16
    titleText.Font = Enum.Font.GothamBold
    titleText.TextXAlignment = Enum.TextXAlignment.Center
    titleText.TextYAlignment = Enum.TextYAlignment.Center
    titleText.Parent = titleBar
    
    -- Кнопка закрытия
    local closeBtn = Instance.new("TextButton")
    closeBtn.Size = UDim2.new(0, 28, 0, 28)
    closeBtn.Position = UDim2.new(1, -36, 0, 6)
    closeBtn.BackgroundColor3 = C.Error
    closeBtn.BackgroundTransparency = 0.2
    closeBtn.BorderSizePixel = 0
    closeBtn.Text = "✕"
    closeBtn.TextColor3 = C.White
    closeBtn.TextSize = 14
    closeBtn.Font = Enum.Font.GothamBold
    closeBtn.Parent = titleBar
    local clCorner = Instance.new("UICorner", closeBtn)
    clCorner.CornerRadius = UDim.new(0, 14)
    closeBtn.MouseButton1Click:Connect(function()
        menuFrame.Visible = false
        menuOpen = false
    end)
    
    -- Панель вкладок
    local tabBar = Instance.new("Frame")
    tabBar.Size = UDim2.new(1, 0, 0, 35)
    tabBar.Position = UDim2.new(0, 0, 0, 40)
    tabBar.BackgroundColor3 = Color3.fromRGB(12, 12, 35)
    tabBar.BackgroundTransparency = 0.3
    tabBar.BorderSizePixel = 0
    tabBar.Parent = menuFrame
    
    local tabNames = {"Auto-Farm", "Farm Quest", "TeleKill", "Secret"}
    local tabW = menuW / #tabNames
    
    for i, name in ipairs(tabNames) do
        local btn = Instance.new("TextButton")
        btn.Size = UDim2.new(0, tabW, 1, 0)
        btn.Position = UDim2.new(0, tabW * (i - 1), 0, 0)
        btn.BackgroundColor3 = i == 1 and C.Primary or Color3.fromRGB(12, 12, 35)
        btn.BackgroundTransparency = i == 1 and 0.3 or 0.7
        btn.BorderSizePixel = 0
        btn.Text = name
        btn.TextColor3 = i == 1 and C.White or C.Gray
        btn.TextSize = 13
        btn.Font = Enum.Font.GothamBold
        btn.Parent = tabBar
        tabButtons[i] = btn
        
        local ind = Instance.new("Frame")
        ind.Size = UDim2.new(1, 0, 0, 2)
        ind.Position = UDim2.new(0, 0, 1, -2)
        ind.BackgroundColor3 = C.Primary
        ind.BackgroundTransparency = i == 1 and 0 or 1
        ind.BorderSizePixel = 0
        ind.Parent = btn
        
        btn.MouseButton1Click:Connect(function()
            if i == 4 and not SECRET_UNLOCKED then
                ShowSecretPopup(function(success)
                    if success then
                        SECRET_UNLOCKED = true
                        for j, b in ipairs(tabButtons) do
                            b.BackgroundColor3 = j == 4 and C.Primary or Color3.fromRGB(12, 12, 35)
                            b.BackgroundTransparency = j == 4 and 0.3 or 0.7
                            b.TextColor3 = j == 4 and C.White or C.Gray
                            local ind2 = b:FindFirstChild("Indicator")
                            if ind2 then ind2.BackgroundTransparency = j == 4 and 0 or 1 end
                        end
                        currentTab = 4
                        LAST_TAB = 4
                        for j, content in ipairs(tabContents) do
                            if content then content.Visible = (j == 4) end
                        end
                    else
                        local prev = LAST_TAB
                        for j, b in ipairs(tabButtons) do
                            b.BackgroundColor3 = j == prev and C.Primary or Color3.fromRGB(12, 12, 35)
                            b.BackgroundTransparency = j == prev and 0.3 or 0.7
                            b.TextColor3 = j == prev and C.White or C.Gray
                            local ind2 = b:FindFirstChild("Indicator")
                            if ind2 then ind2.BackgroundTransparency = j == prev and 0 or 1 end
                        end
                        for j, content in ipairs(tabContents) do
                            if content then content.Visible = (j == prev) end
                        end
                    end
                end)
                return
            end
            
            for j, b in ipairs(tabButtons) do
                b.BackgroundColor3 = j == i and C.Primary or Color3.fromRGB(12, 12, 35)
                b.BackgroundTransparency = j == i and 0.3 or 0.7
                b.TextColor3 = j == i and C.White or C.Gray
                local ind2 = b:FindFirstChild("Indicator")
                if ind2 then ind2.BackgroundTransparency = j == i and 0 or 1 end
            end
            currentTab = i
            LAST_TAB = i
            for j, content in ipairs(tabContents) do
                if content then content.Visible = (j == i) end
            end
        end)
    end
    
    -- Контент
    local contentFrame = Instance.new("Frame")
    contentFrame.Size = UDim2.new(1, -20, 1, -95)
    contentFrame.Position = UDim2.new(0, 10, 0, 85)
    contentFrame.BackgroundColor3 = Color3.fromRGB(10, 10, 32)
    contentFrame.BackgroundTransparency = 0.2
    contentFrame.BorderSizePixel = 0
    contentFrame.ClipsDescendants = true
    contentFrame.Parent = menuFrame
    local cfCorner = Instance.new("UICorner", contentFrame)
    cfCorner.CornerRadius = UDim.new(0, 12)
    
    -- ======== ВКЛАДКА 1: Auto-Farm ========
    local tab1 = Instance.new("Frame")
    tab1.Size = UDim2.new(1, 0, 1, 0)
    tab1.BackgroundTransparency = 1
    tab1.Parent = contentFrame
    
    local label1 = Instance.new("TextLabel")
    label1.Size = UDim2.new(1, 0, 0, 30)
    label1.Position = UDim2.new(0, 10, 0, 5)
    label1.BackgroundTransparency = 1
    label1.Text = "⚔️ AUTO-FARM"
    label1.TextColor3 = C.Primary
    label1.TextSize = 16
    label1.Font = Enum.Font.GothamBold
    label1.TextXAlignment = Enum.TextXAlignment.Left
    label1.Parent = tab1
    
    local desertBtn = Instance.new("TextButton")
    desertBtn.Size = UDim2.new(0.4, 0, 0, 30)
    desertBtn.Position = UDim2.new(0.05, 0, 0.12, 0)
    desertBtn.BackgroundColor3 = C.Orange
    desertBtn.BorderSizePixel = 0
    desertBtn.Text = "🏜️ Teleport Desert"
    desertBtn.TextColor3 = C.White
    desertBtn.TextSize = 12
    desertBtn.Font = Enum.Font.GothamBold
    desertBtn.Parent = tab1
    local dCorner2 = Instance.new("UICorner", desertBtn)
    dCorner2.CornerRadius = UDim.new(0, 6)
    desertBtn.MouseButton1Click:Connect(TeleportToDesert)
    
    local farmBtn = Instance.new("TextButton")
    farmBtn.Size = UDim2.new(0.4, 0, 0, 30)
    farmBtn.Position = UDim2.new(0.5, 5, 0.12, 0)
    farmBtn.BackgroundColor3 = C.Success
    farmBtn.BorderSizePixel = 0
    farmBtn.Text = "▶️ Start Farm"
    farmBtn.TextColor3 = C.White
    farmBtn.TextSize = 12
    farmBtn.Font = Enum.Font.GothamBold
    farmBtn.Parent = tab1
    local fCorner = Instance.new("UICorner", farmBtn)
    fCorner.CornerRadius = UDim.new(0, 6)
    
    local farmActive = false
    farmBtn.MouseButton1Click:Connect(function()
        farmActive = not farmActive
        farmBtn.Text = farmActive and "⏹️ Stop Farm" or "▶️ Start Farm"
        farmBtn.BackgroundColor3 = farmActive and C.Error or C.Success
        if farmActive then
            spawn(function()
                while farmActive do
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name:match("Curse") and obj:FindFirstChild("Humanoid") then
                            local hum = obj.Humanoid
                            if hum.Health > 0 then hum.Health = 0 end
                        end
                        if obj:IsA("Model") and obj.Name:match("Chest") and obj:FindFirstChild("TouchInterest") then
                            if Character and Character.PrimaryPart then
                                Character.PrimaryPart.CFrame = obj.PrimaryPart.CFrame + Vector3.new(0, 2, 0)
                                wait(0.2)
                            end
                        end
                    end
                    wait(0.5)
                end
            end)
        end
    end)
    
    local speedLabel = Instance.new("TextLabel")
    speedLabel.Size = UDim2.new(0.3, 0, 0, 25)
    speedLabel.Position = UDim2.new(0.05, 0, 0.28, 0)
    speedLabel.BackgroundTransparency = 1
    speedLabel.Text = "Speed: 1/s"
    speedLabel.TextColor3 = C.Light
    speedLabel.TextSize = 12
    speedLabel.Font = Enum.Font.Gotham
    speedLabel.TextXAlignment = Enum.TextXAlignment.Left
    speedLabel.Parent = tab1
    
    local speedSlider = Instance.new("Frame")
    speedSlider.Size = UDim2.new(0.4, 0, 0, 6)
    speedSlider.Position = UDim2.new(0.45, 0, 0.30, 0)
    speedSlider.BackgroundColor3 = Color3.fromRGB(30, 30, 70)
    speedSlider.BorderSizePixel = 0
    speedSlider.Parent = tab1
    local sCorner = Instance.new("UICorner", speedSlider)
    sCorner.CornerRadius = UDim.new(0, 3)
    
    local speedFill = Instance.new("Frame")
    speedFill.Size = UDim2.new(0.5, 0, 1, 0)
    speedFill.BackgroundColor3 = C.Primary
    speedFill.BorderSizePixel = 0
    speedFill.Parent = speedSlider
    local sfCorner = Instance.new("UICorner", speedFill)
    sfCorner.CornerRadius = UDim.new(0, 3)
    
    local killSpeed = 1
    speedSlider.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            local pos = input.Position.X - speedSlider.AbsolutePosition.X
            local width = speedSlider.AbsoluteSize.X
            local progress = Clamp(pos / width, 0, 1)
            speedFill.Size = UDim2.new(progress, 0, 1, 0)
            killSpeed = math.floor(progress * 10) + 1
            speedLabel.Text = "Speed: " .. killSpeed .. "/s"
        end
    end)
    
    local chestBtn = Instance.new("TextButton")
    chestBtn.Size = UDim2.new(0.4, 0, 0, 28)
    chestBtn.Position = UDim2.new(0.05, 0, 0.4, 0)
    chestBtn.BackgroundColor3 = C.Purple
    chestBtn.BorderSizePixel = 0
    chestBtn.Text = "📦 Auto-Chest"
    chestBtn.TextColor3 = C.White
    chestBtn.TextSize = 12
    chestBtn.Font = Enum.Font.GothamBold
    chestBtn.Parent = tab1
    local cCorner = Instance.new("UICorner", chestBtn)
    cCorner.CornerRadius = UDim.new(0, 6)
    
    local chestActive = false
    chestBtn.MouseButton1Click:Connect(function()
        chestActive = not chestActive
        chestBtn.BackgroundColor3 = chestActive and C.Success or C.Purple
        chestBtn.Text = chestActive and "✅ Chest ON" or "📦 Auto-Chest"
        if chestActive then
            spawn(function()
                while chestActive do
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name:match("Chest") and obj:FindFirstChild("TouchInterest") then
                            if Character and Character.PrimaryPart then
                                Character.PrimaryPart.CFrame = obj.PrimaryPart.CFrame + Vector3.new(0, 2, 0)
                                wait(0.2)
                            end
                        end
                    end
                    wait(0.5)
                end
            end)
        end
    end)
    
    -- ======== ВКЛАДКА 2: Farm Quest ========
    local tab2 = Instance.new("Frame")
    tab2.Size = UDim2.new(1, 0, 1, 0)
    tab2.BackgroundTransparency = 1
    tab2.Visible = false
    tab2.Parent = contentFrame
    
    local label2 = Instance.new("TextLabel")
    label2.Size = UDim2.new(1, 0, 0, 30)
    label2.Position = UDim2.new(0, 10, 0, 5)
    label2.BackgroundTransparency = 1
    label2.Text = "📜 FARM ABILITIES & ITEMS"
    label2.TextColor3 = C.Secondary
    label2.TextSize = 16
    label2.Font = Enum.Font.GothamBold
    label2.TextXAlignment = Enum.TextXAlignment.Left
    label2.Parent = tab2
    
    local abilityBtn = Instance.new("TextButton")
    abilityBtn.Size = UDim2.new(0.4, 0, 0, 30)
    abilityBtn.Position = UDim2.new(0.05, 0, 0.12, 0)
    abilityBtn.BackgroundColor3 = C.Cyan
    abilityBtn.BorderSizePixel = 0
    abilityBtn.Text = "⚡ Farm Abilities"
    abilityBtn.TextColor3 = C.White
    abilityBtn.TextSize = 12
    abilityBtn.Font = Enum.Font.GothamBold
    abilityBtn.Parent = tab2
    local aCorner = Instance.new("UICorner", abilityBtn)
    aCorner.CornerRadius = UDim.new(0, 6)
    
    local abilityActive = false
    abilityBtn.MouseButton1Click:Connect(function()
        abilityActive = not abilityActive
        abilityBtn.BackgroundColor3 = abilityActive and C.Success or C.Cyan
        abilityBtn.Text = abilityActive and "✅ ON" or "⚡ Farm Abilities"
        if abilityActive then
            spawn(function()
                while abilityActive do
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name:match("Ability") and obj:FindFirstChild("TouchInterest") then
                            if Character and Character.PrimaryPart then
                                Character.PrimaryPart.CFrame = obj.PrimaryPart.CFrame + Vector3.new(0, 2, 0)
                                wait(0.3)
                            end
                        end
                    end
                    wait(1)
                end
            end)
        end
    end)
    
    local rarityLabel = Instance.new("TextLabel")
    rarityLabel.Size = UDim2.new(0.3, 0, 0, 25)
    rarityLabel.Position = UDim2.new(0.05, 0, 0.24, 0)
    rarityLabel.BackgroundTransparency = 1
    rarityLabel.Text = "Rarity: Mythical"
    rarityLabel.TextColor3 = C.Light
    rarityLabel.TextSize = 12
    rarityLabel.Font = Enum.Font.Gotham
    rarityLabel.TextXAlignment = Enum.TextXAlignment.Left
    rarityLabel.Parent = tab2
    
    local rarityBtn = Instance.new("TextButton")
    rarityBtn.Size = UDim2.new(0.25, 0, 0, 25)
    rarityBtn.Position = UDim2.new(0.4, 0, 0.24, 0)
    rarityBtn.BackgroundColor3 = C.Gold
    rarityBtn.BorderSizePixel = 0
    rarityBtn.Text = "Mythical"
    rarityBtn.TextColor3 = C.White
    rarityBtn.TextSize = 11
    rarityBtn.Font = Enum.Font.GothamBold
    rarityBtn.Parent = tab2
    local rCorner = Instance.new("UICorner", rarityBtn)
    rCorner.CornerRadius = UDim.new(0, 5)
    
    local rarities = {"Mythical", "Legendary", "Epic", "Rare"}
    local rarityIndex = 1
    rarityBtn.MouseButton1Click:Connect(function()
        rarityIndex = rarityIndex % #rarities + 1
        rarityBtn.Text = rarities[rarityIndex]
        rarityLabel.Text = "Rarity: " .. rarities[rarityIndex]
    end)
    
    local searchBox = Instance.new("TextBox")
    searchBox.Size = UDim2.new(0.5, 0, 0, 28)
    searchBox.Position = UDim2.new(0.05, 0, 0.35, 0)
    searchBox.BackgroundColor3 = Color3.fromRGB(20, 18, 45)
    searchBox.BackgroundTransparency = 0
    searchBox.BorderSizePixel = 1
    searchBox.BorderColor3 = C.Primary
    searchBox.Text = ""
    searchBox.PlaceholderText = "🔍 Search items..."
    searchBox.PlaceholderColor3 = C.Gray
    searchBox.TextColor3 = C.White
    searchBox.TextSize = 12
    searchBox.Font = Enum.Font.Gotham
    searchBox.Parent = tab2
    local sbCorner = Instance.new("UICorner", searchBox)
    sbCorner.CornerRadius = UDim.new(0, 6)
    
    local itemListFrame = Instance.new("Frame")
    itemListFrame.Size = UDim2.new(0.9, 0, 0.4, 0)
    itemListFrame.Position = UDim2.new(0.05, 0, 0.45, 0)
    itemListFrame.BackgroundColor3 = Color3.fromRGB(15, 12, 38)
    itemListFrame.BackgroundTransparency = 0.3
    itemListFrame.BorderSizePixel = 1
    itemListFrame.BorderColor3 = C.Gray
    itemListFrame.Parent = tab2
    local ilCorner = Instance.new("UICorner", itemListFrame)
    ilCorner.CornerRadius = UDim.new(0, 6)
    
    local itemScroll = Instance.new("ScrollingFrame")
    itemScroll.Size = UDim2.new(1, -10, 1, -10)
    itemScroll.Position = UDim2.new(0, 5, 0, 5)
    itemScroll.BackgroundTransparency = 1
    itemScroll.BorderSizePixel = 0
    itemScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
    itemScroll.ScrollBarThickness = 4
    itemScroll.Parent = itemListFrame
    
    local allItems = GetAUTItems()
    local itemButtons = {}
    
    local function RefreshItems(filter)
        for _, b in ipairs(itemButtons) do b:Destroy() end
        itemButtons = {}
        local y = 0
        for _, item in ipairs(allItems) do
            if not filter or string.lower(item):find(string.lower(filter)) then
                local b = Instance.new("TextButton")
                b.Size = UDim2.new(1, -10, 0, 22)
                b.Position = UDim2.new(0, 5, 0, y)
                b.BackgroundColor3 = Color3.fromRGB(25, 20, 50)
                b.BackgroundTransparency = 0.5
                b.BorderSizePixel = 0
                b.Text = item
                b.TextColor3 = C.Light
                b.TextSize = 11
                b.Font = Enum.Font.Gotham
                b.TextXAlignment = Enum.TextXAlignment.Left
                b.Parent = itemScroll
                local bc = Instance.new("UICorner", b)
                bc.CornerRadius = UDim.new(0, 4)
                table.insert(itemButtons, b)
                y = y + 25
            end
        end
        itemScroll.CanvasSize = UDim2.new(0, 0, 0, y)
    end
    
    RefreshItems()
    searchBox:GetPropertyChangedSignal("Text"):Connect(function()
        RefreshItems(searchBox.Text)
    end)
    
    -- ======== ВКЛАДКА 3: TeleKill ========
    local tab3 = Instance.new("Frame")
    tab3.Size = UDim2.new(1, 0, 1, 0)
    tab3.BackgroundTransparency = 1
    tab3.Visible = false
    tab3.Parent = contentFrame
    
    local label3 = Instance.new("TextLabel")
    label3.Size = UDim2.new(1, 0, 0, 30)
    label3.Position = UDim2.new(0, 10, 0, 5)
    label3.BackgroundTransparency = 1
    label3.Text = "🔫 TELEKILL"
    label3.TextColor3 = C.Error
    label3.TextSize = 16
    label3.Font = Enum.Font.GothamBold
    label3.TextXAlignment = Enum.TextXAlignment.Left
    label3.Parent = tab3
    
    -- TeleBoss
    local bossBtn = Instance.new("TextButton")
    bossBtn.Size = UDim2.new(0.28, 0, 0, 30)
    bossBtn.Position = UDim2.new(0.03, 0, 0.12, 0)
    bossBtn.BackgroundColor3 = C.Orange
    bossBtn.BorderSizePixel = 0
    bossBtn.Text = "👹 TeleBoss"
    bossBtn.TextColor3 = C.White
    bossBtn.TextSize = 11
    bossBtn.Font = Enum.Font.GothamBold
    bossBtn.Parent = tab3
    local bc = Instance.new("UICorner", bossBtn)
    bc.CornerRadius = UDim.new(0, 6)
    
    local bossActive = false
    
    bossBtn.MouseButton1Click:Connect(function()
        bossActive = not bossActive
        bossBtn.BackgroundColor3 = bossActive and C.Success or C.Orange
        bossBtn.Text = bossActive and "✅ ON" or "👹 TeleBoss"
        if bossActive then
            spawn(function()
                while bossActive do
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name:match("Boss") and obj:FindFirstChild("Humanoid") then
                            local hum = obj.Humanoid
                            if hum.Health > 0 and Character and Character.PrimaryPart then
                                local originalPos = obj.PrimaryPart.Position
                                obj.PrimaryPart.CFrame = Character.PrimaryPart.CFrame + Vector3.new(0, 2, 0)
                                hum.Health = 0
                                wait(0.05)
                                obj.PrimaryPart.CFrame = CFrame.new(originalPos)
                            end
                        end
                    end
                    wait(0.3)
                end
            end)
        end
    end)
    
    -- TeleNPC
    local npcBtn = Instance.new("TextButton")
    npcBtn.Size = UDim2.new(0.28, 0, 0, 30)
    npcBtn.Position = UDim2.new(0.35, 3, 0.12, 0)
    npcBtn.BackgroundColor3 = C.Purple
    npcBtn.BorderSizePixel = 0
    npcBtn.Text = "🧟 TeleNPC"
    npcBtn.TextColor3 = C.White
    npcBtn.TextSize = 11
    npcBtn.Font = Enum.Font.GothamBold
    npcBtn.Parent = tab3
    local nc = Instance.new("UICorner", npcBtn)
    nc.CornerRadius = UDim.new(0, 6)
    
    local npcActive = false
    
    npcBtn.MouseButton1Click:Connect(function()
        npcActive = not npcActive
        npcBtn.BackgroundColor3 = npcActive and C.Success or C.Purple
        npcBtn.Text = npcActive and "✅ ON" or "🧟 TeleNPC"
        if npcActive then
            spawn(function()
                while npcActive do
                    for _, obj in ipairs(Workspace:GetDescendants()) do
                        if obj:IsA("Model") and obj.Name:match("NPC") and obj:FindFirstChild("Humanoid") then
                            local hum = obj.Humanoid
                            if hum.Health > 0 and Character and Character.PrimaryPart then
                                local originalPos = obj.PrimaryPart.Position
                                obj.PrimaryPart.CFrame = Character.PrimaryPart.CFrame + Vector3.new(0, 2, 0)
                                hum.Health = 0
                                wait(0.05)
                                obj.PrimaryPart.CFrame = CFrame.new(originalPos)
                            end
                        end
                    end
                    wait(0.3)
                end
            end)
        end
    end)
    
    -- TelePlayer
    local playerBtn = Instance.new("TextButton")
    playerBtn.Size = UDim2.new(0.28, 0, 0, 30)
    playerBtn.Position = UDim2.new(0.67, 3, 0.12, 0)
    playerBtn.BackgroundColor3 = C.Cyan
    playerBtn.BorderSizePixel = 0
    playerBtn.Text = "👤 TelePlayer"
    playerBtn.TextColor3 = C.White
    playerBtn.TextSize = 11
    playerBtn.Font = Enum.Font.GothamBold
    playerBtn.Parent = tab3
    local pc = Instance.new("UICorner", playerBtn)
    pc.CornerRadius = UDim.new(0, 6)
    
    local playerActive = false
    local selectedPlayer = nil
    local playerListFrame = nil
    
    playerBtn.MouseButton1Click:Connect(function()
        playerActive = not playerActive
        playerBtn.BackgroundColor3 = playerActive and C.Success or C.Cyan
        playerBtn.Text = playerActive and "✅ ON" or "👤 TelePlayer"
        
        if playerActive then
            if playerListFrame then playerListFrame:Destroy() end
            
            playerListFrame = Instance.new("Frame")
            playerListFrame.Size = UDim2.new(0.9, 0, 0.5, 0)
            playerListFrame.Position = UDim2.new(0.05, 0, 0.25, 0)
            playerListFrame.BackgroundColor3 = Color3.fromRGB(12, 10, 35)
            playerListFrame.BackgroundTransparency = 0.2
            playerListFrame.BorderSizePixel = 1
            playerListFrame.BorderColor3 = C.Primary
            playerListFrame.Parent = tab3
            local plc = Instance.new("UICorner", playerListFrame)
            plc.CornerRadius = UDim.new(0, 8)
            
            local plScroll = Instance.new("ScrollingFrame")
            plScroll.Size = UDim2.new(1, -10, 1, -50)
            plScroll.Position = UDim2.new(0, 5, 0, 5)
            plScroll.BackgroundTransparency = 1
            plScroll.BorderSizePixel = 0
            plScroll.CanvasSize = UDim2.new(0, 0, 0, 0)
            plScroll.ScrollBarThickness = 4
            plScroll.Parent = playerListFrame
            
            local closePl = Instance.new("TextButton")
            closePl.Size = UDim2.new(0, 28, 0, 28)
            closePl.Position = UDim2.new(1, -34, 0, 5)
            closePl.BackgroundColor3 = C.Error
            closePl.BorderSizePixel = 0
            closePl.Text = "✕"
            closePl.TextColor3 = C.White
            closePl.TextSize = 14
            closePl.Font = Enum.Font.GothamBold
            closePl.Parent = playerListFrame
            local clpc = Instance.new("UICorner", closePl)
            clpc.CornerRadius = UDim.new(0, 14)
            closePl.MouseButton1Click:Connect(function()
                playerListFrame:Destroy()
                playerActive = false
                playerBtn.BackgroundColor3 = C.Cyan
                playerBtn.Text = "👤 TelePlayer"
            end)
            
            local y = 0
            for _, pl in ipairs(Players:GetPlayers()) do
                if pl ~= LocalPlayer then
                    local b = Instance.new("TextButton")
                    b.Size = UDim2.new(1, -10, 0, 22)
                    b.Position = UDim2.new(0, 5, 0, y)
                    b.BackgroundColor3 = Color3.fromRGB(25, 20, 50)
                    b.BackgroundTransparency = 0.5
                    b.BorderSizePixel = 0
                    b.Text = pl.Name
                    b.TextColor3 = C.Light
                    b.TextSize = 11
                    b.Font = Enum.Font.Gotham
                    b.TextXAlignment = Enum.TextXAlignment.Left
                    b.Parent = plScroll
                    local bpc = Instance.new("UICorner", b)
                    bpc.CornerRadius = UDim.new(0, 4)
                    b.MouseButton1Click:Connect(function()
                        selectedPlayer = pl
                        for _, ch in ipairs(plScroll:GetChildren()) do
                            if ch:IsA("TextButton") then
                                ch.BackgroundColor3 = Color3.fromRGB(25, 20, 50)
                                ch.BackgroundTransparency = 0.5
                            end
                        end
                        b.BackgroundColor3 = C.Success
                        b.BackgroundTransparency = 0.3
                    end)
                    y = y + 25
                end
            end
            plScroll.CanvasSize = UDim2.new(0, 0, 0, y)
            
            local teleBtn = Instance.new("TextButton")
            teleBtn.Size = UDim2.new(0.25, 0, 0, 28)
            teleBtn.Position = UDim2.new(0.05, 0, 0.9, 0)
            teleBtn.BackgroundColor3 = C.Primary
            teleBtn.BorderSizePixel = 0
            teleBtn.Text = "📞 Tele!"
            teleBtn.TextColor3 = C.White
            teleBtn.TextSize = 12
            teleBtn.Font = Enum.Font.GothamBold
            teleBtn.Parent = playerListFrame
            local tpc = Instance.new("UICorner", teleBtn)
            tpc.CornerRadius = UDim.new(0, 6)
            teleBtn.MouseButton1Click:Connect(function()
                if selectedPlayer and selectedPlayer.Character and selectedPlayer.Character.PrimaryPart then
                    if Character and Character.PrimaryPart then
                        local originalPos = selectedPlayer.Character.PrimaryPart.Position
                        selectedPlayer.Character.PrimaryPart.CFrame = Character.PrimaryPart.CFrame + Vector3.new(2, 0, 0)
                        wait(0.05)
                        selectedPlayer.Character.PrimaryPart.CFrame = CFrame.new(originalPos)
                    end
                end
            end)
            
            local teleAllBtn = Instance.new("TextButton")
            teleAllBtn.Size = UDim2.new(0.25, 0, 0, 28)
            teleAllBtn.Position = UDim2.new(0.35, 5, 0.9, 0)
            teleAllBtn.BackgroundColor3 = C.Warning
            teleAllBtn.BorderSizePixel = 0
            teleAllBtn.Text = "👥 Tele All"
            teleAllBtn.TextColor3 = C.White
            teleAllBtn.TextSize = 12
            teleAllBtn.Font = Enum.Font.GothamBold
            teleAllBtn.Parent = playerListFrame
            local tac = Instance.new("UICorner", teleAllBtn)
            tac.CornerRadius = UDim.new(0, 6)
            teleAllBtn.MouseButton1Click:Connect(function()
                if Character and Character.PrimaryPart then
                    for _, pl in ipairs(Players:GetPlayers()) do
                        if pl ~= LocalPlayer and pl.Character and pl.Character.PrimaryPart then
                            local originalPos = pl.Character.PrimaryPart.Position
                            pl.Character.PrimaryPart.CFrame = Character.PrimaryPart.CFrame + Vector3.new(2, 0, 0)
                            wait(0.05)
                            pl.Character.PrimaryPart.CFrame = CFrame.new(originalPos)
                        end
                    end
                end
            end)
        else
            if playerListFrame then
                playerListFrame:Destroy()
                playerListFrame = nil
            end
        end
    end)
    
    -- ======== ВКЛАДКА 4: Secret ========
    local tab4 = Instance.new("Frame")
    tab4.Size = UDim2.new(1, 0, 1, 0)
    tab4.BackgroundTransparency = 1
    tab4.Visible = false
    tab4.Parent = contentFrame
    
    local label4 = Instance.new("TextLabel")
    label4.Size = UDim2.new(1, 0, 0, 30)
    label4.Position = UDim2.new(0, 10, 0, 5)
    label4.BackgroundTransparency = 1
    label4.Text = "🔐 SECRET TAB"
    label4.TextColor3 = C.Gold
    label4.TextSize = 16
    label4.Font = Enum.Font.GothamBold
    label4.TextXAlignment = Enum.TextXAlignment.Left
    label4.Parent = tab4
    
    local scamBtn = Instance.new("TextButton")
    scamBtn.Size = UDim2.new(0.4, 0, 0, 35)
    scamBtn.Position = UDim2.new(0.3, 0, 0.12, 0)
    scamBtn.BackgroundColor3 = C.Success
    scamBtn.BorderSizePixel = 0
    scamBtn.Text = "💰 ScamTrade"
    scamBtn.TextColor3 = C.White
    scamBtn.TextSize = 13
    scamBtn.Font = Enum.Font.GothamBold
    scamBtn.Parent = tab4
    local scamCorner = Instance.new("UICorner", scamBtn)
    scamCorner.CornerRadius = UDim.new(0, 8)
    
    local scamActive = false
    local confirmBtn = nil
    local addItemBtn = nil
    
    scamBtn.MouseButton1Click:Connect(function()
        scamActive = not scamActive
        scamBtn.BackgroundColor3 = scamActive and C.Error or C.Success
        scamBtn.Text = scamActive and "⏹️ Stop" or "💰 ScamTrade"
        
        if scamActive then
            confirmBtn = Instance.new("TextButton")
            confirmBtn.Size = UDim2.new(0.4, 0, 0, 35)
            confirmBtn.Position = UDim2.new(0.3, 0, 0.35, 0)
            confirmBtn.BackgroundColor3 = Color3.fromRGB(0, 200, 0)
            confirmBtn.BorderSizePixel = 2
            confirmBtn.BorderColor3 = Color3.fromRGB(0, 255, 0)
            confirmBtn.Text = "✅ CONFIRM!"
            confirmBtn.TextColor3 = C.White
            confirmBtn.TextSize = 16
            confirmBtn.Font = Enum.Font.GothamBold
            confirmBtn.Parent = tab4
            local confCorner = Instance.new("UICorner", confirmBtn)
            confCorner.CornerRadius = UDim.new(0, 10)
            
            addItemBtn = Instance.new("TextButton")
            addItemBtn.Size = UDim2.new(0.3, 0, 0, 28)
            addItemBtn.Position = UDim2.new(0.05, 0, 0.55, 0)
            addItemBtn.BackgroundColor3 = C.Purple
            addItemBtn.BorderSizePixel = 0
            addItemBtn.Text = "➕ Add Fake Item"
            addItemBtn.TextColor3 = C.White
            addItemBtn.TextSize = 11
            addItemBtn.Font = Enum.Font.GothamBold
            addItemBtn.Parent = tab4
            local aiCorner = Instance.new("UICorner", addItemBtn)
            aiCorner.CornerRadius = UDim.new(0, 6)
            
            local fakeItems = {}
            local itemDisplay = nil
            
            addItemBtn.MouseButton1Click:Connect(function()
                local itemList = GetAUTItems()
                local item = itemList[math.random(1, #itemList)]
                table.insert(fakeItems, item)
                
                if itemDisplay then itemDisplay:Destroy() end
                itemDisplay = Instance.new("TextLabel")
                itemDisplay.Size = UDim2.new(0.5, 0, 0, 20)
                itemDisplay.Position = UDim2.new(0.05, 0, 0.62, 0)
                itemDisplay.BackgroundTransparency = 1
                itemDisplay.Text = "✨ Added: " .. table.concat(fakeItems, ", ")
                itemDisplay.TextColor3 = C.Gold
                itemDisplay.TextSize = 11
                itemDisplay.Font = Enum.Font.Gotham
                itemDisplay.TextXAlignment = Enum.TextXAlignment.Left
                itemDisplay.Parent = tab4
            end)
            
            confirmBtn.MouseButton1Click:Connect(function()
                for _, pl in ipairs(Players:GetPlayers()) do
                    if pl ~= LocalPlayer and pl.Character then
                        local fakeGui = Instance.new("ScreenGui")
                        fakeGui.Name = "FakeTradeGUI"
                        fakeGui.Parent = pl.PlayerGui
                        local fakeFrame = Instance.new("Frame")
                        fakeFrame.Size = UDim2.new(0.3, 0, 0.3, 0)
                        fakeFrame.Position = UDim2.new(0.35, 0, 0.35, 0)
                        fakeFrame.BackgroundColor3 = Color3.fromRGB(0, 0, 0)
                        fakeFrame.BackgroundTransparency = 0.2
                        fakeFrame.BorderSizePixel = 2
                        fakeFrame.BorderColor3 = C.Gold
                        fakeFrame.Parent = fakeGui
                        local ffCorner = Instance.new("UICorner", fakeFrame)
                        ffCorner.CornerRadius = UDim.new(0, 10)
                        local fakeText = Instance.new("TextLabel")
                        fakeText.Size = UDim2.new(1, 0, 1, 0)
                        fakeText.BackgroundTransparency = 1
                        fakeText.Text = "⚡ TRADE FROZEN\nItems: " .. table.concat(fakeItems, ", ")
                        fakeText.TextColor3 = C.Gold
                        fakeText.TextSize = 13
                        fakeText.Font = Enum.Font.GothamBold
                        fakeText.TextWrapped = true
                        fakeText.Parent = fakeFrame
                        wait(5)
                        fakeGui:Destroy()
                    end
                end
                if confirmBtn then confirmBtn:Destroy() end
                if addItemBtn then addItemBtn:Destroy() end
                scamActive = false
                scamBtn.BackgroundColor3 = C.Success
                scamBtn.Text = "💰 ScamTrade"
            end)
        else
            if confirmBtn then confirmBtn:Destroy() end
            if addItemBtn then addItemBtn:Destroy() end
        end
    end)
    
    -- Сохраняем вкладки
    tabContents = {tab1, tab2, tab3, tab4}
    
    -- Перетаскивание
    local dragging = false
    local dragStart = nil
    local frameStart = nil
    
    titleBar.InputBegan:Connect(function(input)
        if input.UserInputType == Enum.UserInputType.MouseButton1 then
            dragging = true
            dragStart = input.Position
            frameStart = menuFrame.Position
        end
    end)
    
    UserInputService.InputChanged:Connect(function(input)
        if dragging and input.UserInputType == Enum.UserInputType.MouseMovement then
            local delta = input.Position - dragStart
            local newX = frameStart.X.Offset + delta.X
            local newY = frameStart.Y.Offset + delta.Y
            newX = Clamp(newX, -menuFrame.AbsoluteSize.X + 100, ScreenGui.AbsoluteSize.X - 100)
            newY = Clamp(newY, 0, ScreenGui.AbsoluteSize.Y - 60)
            menuFrame.Position = UDim2.new(0, newX, 0, newY)
        end
    end)
    
    UserInputService.InputEnded:Connect(function(input)
        if dragging then
            dragging = false
        end
    end)
    
    print("[SeraphiCA] Menu created successfully")
    return menuFrame
end

-- ============================================================
--  ПОПАП ДЛЯ СЕКРЕТНОЙ ВКЛАДКИ
-- ============================================================
function ShowSecretPopup(callback)
    if secretPopup then return end
    
    secretPopup = Instance.new("Frame")
    secretPopup.Size = UDim2.new(0, 400, 0, 250)
    secretPopup.Position = UDim2.new(0.5, -200, 0.5, -125)
    secretPopup.BackgroundColor3 = Color3.fromRGB(15, 8, 40)
    secretPopup.BackgroundTransparency = 0
    secretPopup.BorderSizePixel = 2
    secretPopup.BorderColor3 = C.Purple
    secretPopup.ZIndex = 300
    secretPopup.Parent = ScreenGui
    local spCorner = Instance.new("UICorner", secretPopup)
    spCorner.CornerRadius = UDim.new(0, 16)
    
    local spGrad = Instance.new("UIGradient")
    spGrad.Color = ColorSequence.new({
        ColorSequenceKeypoint.new(0, Color3.fromRGB(30, 10, 60)),
        ColorSequenceKeypoint.new(0.5, Color3.fromRGB(20, 5, 40)),
        ColorSequenceKeypoint.new(1, Color3.fromRGB(30, 10, 60))
    })
    spGrad.Rotation = 45
    spGrad.Parent = secretPopup
    
    local title = Instance.new("TextLabel")
    title.Size = UDim2.new(1, 0, 0, 45)
    title.BackgroundTransparency = 1
    title.Text = "🔐 SECRET ACCESS"
    title.TextColor3 = C.Purple
    title.TextSize = 22
    title.Font = Enum.Font.GothamBold
    title.TextXAlignment = Enum.TextXAlignment.Center
    title.TextYAlignment = Enum.TextYAlignment.Center
    title.TextStrokeTransparency = 0.3
    title.TextStrokeColor3 = C.Purple
    title.Parent = secretPopup
    
    local subTitle = Instance.new("TextLabel")
    subTitle.Size = UDim2.new(1, 0, 0, 22)
    subTitle.Position = UDim2.new(0, 0, 0.2, 0)
    subTitle.BackgroundTransparency = 1
    subTitle.Text = "Enter the secret key to unlock"
    subTitle.TextColor3 = C.Gray
    subTitle.TextSize = 12
    subTitle.Font = Enum.Font.Gotham
    subTitle.TextXAlignment = Enum.TextXAlignment.Center
    subTitle.Parent = secretPopup
    
    local secretInput = Instance.new("TextBox")
    secretInput.Size = UDim2.new(0.8, 0, 0, 40)
    secretInput.Position = UDim2.new(0.1, 0, 0.35, 0)
    secretInput.BackgroundColor3 = Color3.fromRGB(25, 15, 50)
    secretInput.BackgroundTransparency = 0
    secretInput.BorderSizePixel = 2
    secretInput.BorderColor3 = C.Purple
    secretInput.Text = ""
    secretInput.PlaceholderText = "Enter access key..."
    secretInput.PlaceholderColor3 = C.DimGray
    secretInput.TextColor3 = C.White
    secretInput.TextSize = 16
    secretInput.Font = Enum.Font.Gotham
    secretInput.Parent = secretPopup
    local siCorner = Instance.new("UICorner", secretInput)
    siCorner.CornerRadius = UDim.new(0, 10)
    
    local submitBtn = Instance.new("TextButton")
    submitBtn.Size = UDim2.new(0.4, 0, 0, 38)
    submitBtn.Position = UDim2.new(0.3, 0, 0.6, 0)
    submitBtn.BackgroundColor3 = C.Purple
    submitBtn.BackgroundTransparency = 0
    submitBtn.BorderSizePixel = 0
    submitBtn.Text = "🔓 UNLOCK"
    submitBtn.TextColor3 = C.White
    submitBtn.TextSize = 15
    submitBtn.Font = Enum.Font.GothamBold
    submitBtn.Parent = secretPopup
    local sBtnCorner = Instance.new("UICorner", submitBtn)
    sBtnCorner.CornerRadius = UDim.new(0, 10)
    
    local secretStatus = Instance.new("TextLabel")
    secretStatus.Size = UDim2.new(1, 0, 0, 25)
    secretStatus.Position = UDim2.new(0, 0, 0.82, 0)
    secretStatus.BackgroundTransparency = 1
    secretStatus.Text = "Attempts: " .. ATTEMPTS .. "/" .. MAX_ATTEMPTS
    secretStatus.TextColor3 = C.Light
    secretStatus.TextSize = 13
    secretStatus.Font = Enum.Font.Gotham
    secretStatus.TextXAlignment = Enum.TextXAlignment.Center
    secretStatus.Parent = secretPopup
    
    local spCloseBtn = Instance.new("TextButton")
    spCloseBtn.Size = UDim2.new(0, 32, 0, 32)
    spCloseBtn.Position = UDim2.new(1, -38, 0, 8)
    spCloseBtn.BackgroundColor3 = C.Error
    spCloseBtn.BackgroundTransparency = 0.2
    spCloseBtn.BorderSizePixel = 0
    spCloseBtn.Text = "✕"
    spCloseBtn.TextColor3 = C.White
    spCloseBtn.TextSize = 16
    spCloseBtn.Font = Enum.Font.GothamBold
    spCloseBtn.Parent = secretPopup
    local spcCorner = Instance.new("UICorner", spCloseBtn)
    spcCorner.CornerRadius = UDim.new(0, 16)
    spCloseBtn.MouseButton1Click:Connect(function()
        secretPopup:Destroy()
        secretPopup = nil
        ATTEMPTS = 0
        if callback then callback(false) end
    end)
    
    submitBtn.MouseButton1Click:Connect(function()
        local input = secretInput.Text
        local isValid = false
        if input == DecryptKey(ENCRYPTED_KEY) then
            isValid = true
        end
        if isValid then
            secretStatus.Text = "✅ ACCESS GRANTED!"
            secretStatus.TextColor3 = C.Success
            wait(0.5)
            secretPopup:Destroy()
            secretPopup = nil
            ATTEMPTS = 0
            if callback then callback(true) end
        else
            ATTEMPTS = ATTEMPTS + 1
            secretStatus.Text = "❌ Wrong key! Attempts: " .. ATTEMPTS .. "/" .. MAX_ATTEMPTS
            secretStatus.TextColor3 = C.Error
            secretInput.Text = ""
            if ATTEMPTS >= MAX_ATTEMPTS then
                secretStatus.Text = "🔒 ACCESS BLOCKED!"
                secretStatus.TextColor3 = C.Error
                submitBtn.Visible = false
                secretInput.Visible = false
                wait(2)
                secretPopup:Destroy()
                secretPopup = nil
                ATTEMPTS = 0
                if callback then callback(false) end
            end
        end
    end)
end

-- ============================================================
--  ЗАПУСК
-- ============================================================
local function Main()
    print("[SeraphiCA] Starting...")
    wait(0.5)
    
    -- Создаём меню
    local menu = CreateMenu()
    
    Watermark.MouseButton1Click:Connect(function()
        if menuOpen then
            menu.Visible = false
            menuOpen = false
        else
            menu.Visible = true
            menuOpen = true
            menu.Size = UDim2.new(0, 0, 0, 0)
            TweenService:Create(menu, TweenInfo.new(0.5, Enum.EasingStyle.Back, Enum.EasingDirection.Out), {
                Size = UDim2.new(0, 620, 0, 420)
            }):Play()
        end
    end)
    
    print("[SeraphiCA] ✅ Script loaded! Click watermark to open menu.")
    print("[SeraphiCA] 🔑 Secret password: 081324")
end

pcall(Main)
print("[SeraphiCA] Script execution finished")
